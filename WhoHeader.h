#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>  // For isspace

void open_database ( char *filename);
int lookup_user_names (char *user_name);
void close_database (void);
void made(int index);

